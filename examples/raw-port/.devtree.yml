name: raw-port-app
worktreesRoot: ../raw-port-app-worktrees

setup:
  - name: install dependencies
    run: npm install

apps:
  web:
    cwd: .
    command: npm run dev
    url:
      provider: raw-port
      url: http://127.0.0.1:3000
    healthUrl: /
